<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CipherNote" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Prevent white flash -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="Theme.CipherNote.NoSplash" parent="Theme.CipherNote">
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>